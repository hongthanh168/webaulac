@using System.Web.Mvc
@model bool?
@{
    var selectList = new List<SelectListItem>();
    selectList.Add(new SelectListItem { Text = "Đang thực hiện", Value = String.Empty, Selected = !Model.HasValue });
    selectList.Add(new SelectListItem { Text = "Đã thực hiện", Value = "true", Selected = Model.HasValue && Model.Value });
    selectList.Add(new SelectListItem { Text = "Không thực hiện", Value = "false", Selected = Model.HasValue && !Model.Value });}
@Html.DropDownListFor(m => m, selectList, htmlAttributes: new { @class = "form-control" })
